- content_for :page_title do
  %h1.header Add a Server
  %p Add a new server for us to monitor.

.row
  .span8

    = form_for(@server, :html => { :class => 'form-horizontal' }) do |f|
      = render 'form', { :f => f }

      %fieldset
        %legend Start Monitoring Straight Away ?
        %p
          You can add monitoring now and then configure it later via the Monitors tab. 
          We'll add the monitor to receive notification after 5 minutes 

        .control-group
          = check_box_tag :measure, {}, checked_value = @checked
          Monitor this server with 
          = collection_select(:monitor_server, :measure_id, @measures, :id, :name)

      .form-actions
        %button.btn.btn-primary{:type => 'submit', :name => 'connect'} Connect Now
        or
        = link_to 'Back to Servers', servers_path, :class => 'btn'

  .span4
    = render 'sidebar'#, :server => @server
