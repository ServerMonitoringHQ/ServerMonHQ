- content_for :page_title do
  %h1.header 
    Configure The Agent For
    = @server.name
  %p Follow the steps below to start collecting data from your server

.row
  .span8
    %h2
      1. Edit your crontab
    %code
      crontab -e
    %br
    %br
    %h2
      2. Add the following entry.
    %code
      curl -L -s 
      = "curl -L -s #{request.protocol + request.host_with_port}/pulse/#{@server.access_key} | rbash -s stable"
    %br
    %br
    %h2
      3. That's it.
