.row
  .span11
    %h1 Edit Server

    = form_for(@server) do |f|
      = render 'form', { :f => f }

      .actions
        %button.btn.primary{:type => 'submit', :name => 'connect'} Connect Now
        or
        = link_to 'Back to Servers', servers_path, :class => 'btn'

  .span5
    = render 'sidebar'#, :server => @server