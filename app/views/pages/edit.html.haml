- content_for :title do
  Web Page Monitoring
  
.yui-gc.inner-page.add-space
  .yui-u.first
    - form_for(@page, :html => { :class => 'data-entry'}, :url => server_page_path(@server, @page)) do |f|
      = render :partial => 'form', :locals => { :f => f }
      %fieldset.buttons
        = f.submit 'Update', :class => 'awesome'
        or
        = link_to 'Back', server_pages_path(@server)
  .yui-u
    = render :partial => 'example'
