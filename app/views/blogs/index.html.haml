.container
  .row
    .span12.columns
      %h1{ :style => 'margin-top: 40px; font-size: 20px' }
        Blog

      %ul.posts
        - @front_posts.each do |post|
          = render 'post', :post => post
        
    .span4.columns
      / = render 'sidebar'